代理模式，式如其名——在某些情况下，出于种种考虑/限制，一个对象**不能直接访问**另一个对象，需要一个**第三者**（代理）牵线搭桥从而间接达到访问目的，这样的模式就是代理模式。    
比如vpn  是  dns查询的时候，比如google的ip被ban了，所以访问不到
但是vpn  就是  给对应的ip一个代理，这个新ip不在ban内
es6 中 的 proxy
比如事件委托   对多个子元素的监听可以放到父元素上
图片懒加载指的是到视口了才加载，图片预加载是通过替换
比如说缓存代理  -- 用map存储之前计算过的数据，用事件换空间