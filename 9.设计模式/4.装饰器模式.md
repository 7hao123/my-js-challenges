装饰器模式，又名装饰者模式。它的定义是“在不改变原对象的基础上，通过对其进行包装拓展，使原有对象可以满足用户的更复杂需求”。

场景：
1.比如说，给页面的每一个按钮加上一个弹窗（如果做了一次修改，不采用装饰器模式），需要把所有地方都改了
2.比如说加这个报告里面的actionMenu
3.比如说埋点？

所以为了不被已有的业务逻辑干扰，当务之急就是将旧逻辑与新逻辑分离，**把旧逻辑抽出去**：
当需要拓展的时候，就把原来的新逻辑修改了就行   

ES7中的实现 @decorator

常见实现  比如  react中的HOC